#pragma once

namespace LineProtocol {
    struct line_protocol {
    };

    int line_protocol_format(struct line_protocol *line_protocol) {
        return 0;
    }

    int line_protocol_parse(struct line_protocol *line_protocol, char *data, size_t data_size) {
        return 0;
    }
}